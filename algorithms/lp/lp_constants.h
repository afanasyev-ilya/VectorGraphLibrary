#pragma once

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

enum GpuActiveConditionType
{
    AlwaysActive = 0,
    ActivePassiveInner = 1,
    LabelChangedOnPreviousIteration = 2,
    LabelChangedRecently = 3
};

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
