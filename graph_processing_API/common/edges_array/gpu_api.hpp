#pragma once

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#ifdef __USE_GPU__
template <typename _T>
void EdgesArray<_T>::move_to_device()
{
    MemoryAPI::move_array_to_device(edges_data, total_array_size);
}
#endif

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#ifdef __USE_GPU__
template <typename _T>
void EdgesArray<_T>::move_to_host()
{
    MemoryAPI::move_array_to_host(edges_data, total_array_size);
}
#endif

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
