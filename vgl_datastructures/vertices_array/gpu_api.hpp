#pragma once

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#ifdef __USE_GPU__
template <typename _T>
void VerticesArray<_T>::move_to_device()
{
    MemoryAPI::move_array_to_device(vertices_data, vertices_count);
}
#endif

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#ifdef __USE_GPU__
template <typename _T>
void VerticesArray<_T>::move_to_host()
{
    MemoryAPI::move_array_to_host(vertices_data, vertices_count);
}
#endif

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
